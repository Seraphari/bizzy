<nav class="navbar navbar-expand-lg sticky-top h-75" style="background-color: #AABA9E;">
  <div class="container-fluid" >
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb mt-4">
      <p class="text-white">Your sectors: &nbsp &nbsp</p>
        <% @current_user.founder.sectors.each do | sector | %>
          <li class="breadcrumb-item">
            <span class="fw-bold default-button"><a href= <%= "/?sector=#{sector.name}" %>><%= sector.name %></a></span>
          </li>
        <% end %>
         &nbsp
         &nbsp
         &nbsp
      <p class="text-white">Your stage: &nbsp &nbsp</p>
        <li class="breadcrumb-item">
          <span class="fw-bold default-button"><a href= <%= "/?funding_stage=#{@current_user.founder.funding_stage}" %>><%= @current_user.founder.funding_stage %></a></span>
        </li>
      </ol>
    </nav>
  </div>
</nav>
